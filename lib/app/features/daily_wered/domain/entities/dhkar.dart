class DhkarEntity {
  final int? id;
  final String? dhkar;
  final int? no_of_repeating;

  DhkarEntity({this.id, this.dhkar, this.no_of_repeating});
  // final int ? id;
  // final int ? id;
  // final int ? id;
}
