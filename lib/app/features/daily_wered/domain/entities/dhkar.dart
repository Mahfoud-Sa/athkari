class DhkarEntity {
  final int? id;
  final String? dhkar;
  final int? repetitions;

  DhkarEntity({this.id, this.dhkar, this.repetitions});
  // final int ? id;
  // final int ? id;
  // final int ? id;
}
